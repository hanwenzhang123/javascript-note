1.
//tsconfig.json
{
    "compilerOptions": {
        "target": "ESNext",
        "outDir": "dist"
    }
}

2.
inside src folder create a model.ts file

3.
brew install typescript
tsc -v -> check if tsc has installed successfully
tsc -w => w means watching, it watches for any changes
 
